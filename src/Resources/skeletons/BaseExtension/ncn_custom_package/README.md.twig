# {{ package.websiteTitle }} TYPO3 Package

## first install without existing server
```bash
ddev start
ddev composer install --no-scripts
ddev exec touch public/FIRST_INSTALL
ddev launch typo3/install.php
```

## quick install with exisiting server 

# quick install

* `ddev start`
* `ddev composer install --no-scripts`

# sync db + files with live
* `ddev pull live --skip-db`
* `ddev pull live`

## push all from server to github 
```bash
git init
git add *
git commit -m "first commit"
git branch -M main
git remote add origin {{ package.repositoryUrl }}
git push -u origin main
```