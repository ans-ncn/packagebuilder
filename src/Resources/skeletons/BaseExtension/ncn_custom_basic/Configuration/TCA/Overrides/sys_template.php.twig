<?php
{% if package.typo3version < 11000000 %}defined('TYPO3_MODE') || die();{% else %}defined('TYPO3') or die('Access denied.');{% endif %}

call_user_func(function()
{
    /**
     * Temporary variables
     */
    $extensionKey = 'ncn_{{ package.packageNameAlternative }}_basic';

    /**
     * Default TypoScript for ncn_{{ package.packageNameAlternative }}_basic
     */
    \TYPO3\CMS\Core\Utility\ExtensionManagementUtility::addStaticFile(
        $extensionKey,
        'Configuration/TsConfig/Page/All.tsconfig',
        '{{ package.packageNameAlternative }} Multisite Package'
    );
});
